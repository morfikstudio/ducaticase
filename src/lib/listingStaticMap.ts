/**
 * Zoom for Google Static Maps on listing pages (1–21). Lower = wider area.
 * Sent as the `z` query param so browser/CDN cache keys update when this changes.
 */
export const LISTING_STATIC_MAP_ZOOM = 15
